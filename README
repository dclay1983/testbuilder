V2.0
detectNetwork function detects which card network a specified card number is part of. detectNetwork.test function tests if all possible variations of a card will work with said networks.
TODO: create card networks as objects with properties of prefix, length and test functions that can be called on a cardNumber. This will make adding networks easier. -done